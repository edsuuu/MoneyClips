# === DESATIVADO no monorepo ===
# O CI/CD próprio deste microserviço foi comentado; o GitHub Actions
# só lê .github/workflows na RAIZ do repo. Será reavaliado depois.
# Conteúdo original preservado abaixo (comentado):
#
# name: CI
# 
# # Valida toda PR para a main e todo push fora da main. É o gate que o
# # auto-merge espera passar antes de mesclar.
# on:
#   push:
#     branches-ignore: [main]
#   pull_request:
#     branches: [main]
# 
# jobs:
#   validate:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
# 
#       - uses: pnpm/action-setup@v4
#         with:
#           version: 10
# 
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
# 
#       # --ignore-scripts pula o download do Chromium (postinstall): typecheck e
#       # lint não precisam do navegador, e isso deixa o CI rápido.
#       - run: pnpm install --frozen-lockfile --ignore-scripts
# 
#       - run: pnpm run typecheck
# 
#       - run: pnpm run lint
